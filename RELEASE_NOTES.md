# FF14 物价百科桌面端 v1.0.5 更新说明

## 2026-05-25

本次更新重点补齐国服大区筛选和 NPC 商店来源信息。

- 新增大区筛选按钮，可按 `陆行鸟`、`莫古力`、`猫小胖`、`豆豆柴` 查看市场价格
- 市场总览和世界服价格表会跟随当前大区筛选刷新
- 普通 NPC 商店来源现在显示 NPC 售价、NPC 名称、地图位置
- 能从数据源确认的 NPC 位置会精确显示到 `X:多少 Y:多少`
- 对缺少明确 NPC 或缺少坐标的数据记录，不再显示“待确认 NPC”，避免误导
- 更新前端脚本缓存版本，减少旧页面缓存继续生效的情况

## 验证

- `node --check app.js`
- Chrome headless 烟测橙汁页面，确认沙沙梅洛显示 `黑衣森林 / 格里达尼亚旧街 / X:14.5 Y:8.5`

## 2026-05-22

本次上传以代码清理和可调性增强为主，搜索链路本身没有改动。

- 清理重复的 `bootstrap`、搜索结果渲染和历史覆盖逻辑
- 收敛当前生效的市场总览、价格表与 Wiki 兜底逻辑
- 保持原有搜索链路不变，继续优先使用双语映射表
- 修复关联物品点击后详情页偶发显示英文名的问题，双语映射命中后统一优先显示中文名
- 修复搜索联想结果层遮挡搜索栏的问题，联想面板位置与间距重新整理
- 主题调色改为支持颜色盘和 `RGB` 颜色代码输入
- 桌面端透明度改为 `10%` 到 `100%` 自由滑杆调节
- 同步整理桌面端与网页端前端实现

## 验证

- `node --check app.js`
- `dotnet build desktop/FF14MarketDesktop/FF14MarketDesktop.csproj -c Release`
